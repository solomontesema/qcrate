#!/usr/bin/env python3
"""Q-Crate APB register smoke tool for KV260 Linux."""
from __future__ import annotations

import argparse
import ctypes
import mmap
import os
import sys
from dataclasses import dataclass

APB_BASE = 0xA001_0000
APB_SIZE = 0x0001_0000

DEVICE_ID_QCRT = 0x5143_5254
VERSION_EXPECTED = 0x0000_0100
STREAM_CLOCK_HZ = 200_000_000
CONTROL_CLOCK_HZ = 100_000_000
CLOCK_RATE_TOLERANCE_HZ = 1_000

FPGA_FIRMWARE_NAME = "qcrate_kv260.bit.bin"
FPGA_FIRMWARE_NAME_PATH = "/proc/device-tree/fpga-region/firmware-name"
CLOCK_RATE_PATHS = (
    ("pl_clk0", "/sys/devices/platform/axi/axi:clocking0/set_rate", STREAM_CLOCK_HZ),
    ("pl_clk1", "/sys/devices/platform/axi/axi:clocking1/set_rate", CONTROL_CLOCK_HZ),
)

SYS_DEVICE_ID = 0x0000
SYS_VERSION = 0x0004
SYS_BUILD_ID = 0x0008
SYS_CAPABILITIES = 0x000C
SYS_SCRATCH = 0x0010
SYS_STREAM_CLOCK_HZ = 0x0014
SYS_CONTROL_CLOCK_HZ = 0x0018

STREAM_BASE = 0x1000
STREAM_CONTROL = STREAM_BASE + 0x000
STREAM_FRAME_LENGTH = STREAM_BASE + 0x004
STREAM_FRAME_COUNT = STREAM_BASE + 0x008
STREAM_MODE = STREAM_BASE + 0x00C
STREAM_STATUS = STREAM_BASE + 0x010
STREAM_COMPLETED_FRAMES = STREAM_BASE + 0x014
STREAM_CURRENT_FRAME_ID = STREAM_BASE + 0x018
STREAM_CURRENT_SAMPLE_INDEX = STREAM_BASE + 0x01C
STREAM_STALL_CYCLES = STREAM_BASE + 0x020
STREAM_IRQ_STATUS = STREAM_BASE + 0x024
STREAM_IRQ_ENABLE = STREAM_BASE + 0x028
STREAM_IRQ_CLEAR = STREAM_BASE + 0x02C
STREAM_TRIGGER_SHOT_ID = STREAM_BASE + 0x030
STREAM_TRIGGER_COUNT = STREAM_BASE + 0x034
STREAM_MISSED_TRIGGER_COUNT = STREAM_BASE + 0x038
STREAM_TRIGGER_TIME_LOW = STREAM_BASE + 0x03C
STREAM_TRIGGER_TIME_HIGH = STREAM_BASE + 0x040
STREAM_FIRST_SAMPLE_TIME_LOW = STREAM_BASE + 0x044
STREAM_FIRST_SAMPLE_TIME_HIGH = STREAM_BASE + 0x048

SEQUENCE_BASE = 0x2000
SEQUENCE_CONTROL = SEQUENCE_BASE + 0x000
SEQUENCE_STATUS = SEQUENCE_BASE + 0x004
SEQUENCE_EVENT_COUNT = SEQUENCE_BASE + 0x008
SEQUENCE_ACTIVE_EVENT = SEQUENCE_BASE + 0x00C
SEQUENCE_COMPLETED_SHOTS = SEQUENCE_BASE + 0x010
SEQUENCE_FAULT_INFO = SEQUENCE_BASE + 0x014
SEQUENCE_TIMEBASE_LO = SEQUENCE_BASE + 0x018
SEQUENCE_TIMEBASE_HI = SEQUENCE_BASE + 0x01C
SEQUENCE_START_TIME_LO = SEQUENCE_BASE + 0x020
SEQUENCE_START_TIME_HI = SEQUENCE_BASE + 0x024
SEQUENCE_ELAPSED_LO = SEQUENCE_BASE + 0x028
SEQUENCE_ELAPSED_HI = SEQUENCE_BASE + 0x02C
SEQUENCE_PAGE_END = SEQUENCE_BASE + 0x1000


@dataclass(frozen=True)
class Register:
    name: str
    offset: int


REGISTERS = [
    Register("SYS.DEVICE_ID", SYS_DEVICE_ID),
    Register("SYS.VERSION", SYS_VERSION),
    Register("SYS.BUILD_ID", SYS_BUILD_ID),
    Register("SYS.CAPABILITIES", SYS_CAPABILITIES),
    Register("SYS.SCRATCH", SYS_SCRATCH),
    Register("SYS.STREAM_CLOCK_HZ", SYS_STREAM_CLOCK_HZ),
    Register("SYS.CONTROL_CLOCK_HZ", SYS_CONTROL_CLOCK_HZ),
    Register("STREAM.CONTROL", STREAM_CONTROL),
    Register("STREAM.FRAME_LENGTH", STREAM_FRAME_LENGTH),
    Register("STREAM.FRAME_COUNT", STREAM_FRAME_COUNT),
    Register("STREAM.STREAM_MODE", STREAM_MODE),
    Register("STREAM.STATUS", STREAM_STATUS),
    Register("STREAM.COMPLETED_FRAMES", STREAM_COMPLETED_FRAMES),
    Register("STREAM.CURRENT_FRAME_ID", STREAM_CURRENT_FRAME_ID),
    Register("STREAM.CURRENT_SAMPLE_INDEX", STREAM_CURRENT_SAMPLE_INDEX),
    Register("STREAM.STALL_CYCLES", STREAM_STALL_CYCLES),
    Register("STREAM.IRQ_STATUS", STREAM_IRQ_STATUS),
    Register("STREAM.IRQ_ENABLE", STREAM_IRQ_ENABLE),
    Register("STREAM.TRIGGER_SHOT_ID", STREAM_TRIGGER_SHOT_ID),
    Register("STREAM.TRIGGER_COUNT", STREAM_TRIGGER_COUNT),
    Register("STREAM.MISSED_TRIGGER_COUNT", STREAM_MISSED_TRIGGER_COUNT),
    Register("STREAM.TRIGGER_TIME_LOW", STREAM_TRIGGER_TIME_LOW),
    Register("STREAM.TRIGGER_TIME_HIGH", STREAM_TRIGGER_TIME_HIGH),
    Register("STREAM.FIRST_SAMPLE_TIME_LOW", STREAM_FIRST_SAMPLE_TIME_LOW),
    Register("STREAM.FIRST_SAMPLE_TIME_HIGH", STREAM_FIRST_SAMPLE_TIME_HIGH),
    Register("SEQUENCE.CONTROL", SEQUENCE_CONTROL),
    Register("SEQUENCE.STATUS", SEQUENCE_STATUS),
    Register("SEQUENCE.EVENT_COUNT", SEQUENCE_EVENT_COUNT),
    Register("SEQUENCE.ACTIVE_EVENT", SEQUENCE_ACTIVE_EVENT),
    Register("SEQUENCE.COMPLETED_SHOTS", SEQUENCE_COMPLETED_SHOTS),
    Register("SEQUENCE.FAULT_INFO", SEQUENCE_FAULT_INFO),
    Register("SEQUENCE.TIMEBASE_LO", SEQUENCE_TIMEBASE_LO),
    Register("SEQUENCE.TIMEBASE_HI", SEQUENCE_TIMEBASE_HI),
    Register("SEQUENCE.START_TIME_LO", SEQUENCE_START_TIME_LO),
    Register("SEQUENCE.START_TIME_HI", SEQUENCE_START_TIME_HI),
    Register("SEQUENCE.ELAPSED_LO", SEQUENCE_ELAPSED_LO),
    Register("SEQUENCE.ELAPSED_HI", SEQUENCE_ELAPSED_HI),
]


class DevMem32:
    def __init__(self, base: int, size: int) -> None:
        if sys.byteorder != "little":
            raise RuntimeError("Q-Crate MMIO tools require a little-endian CPU")
        if base % ctypes.sizeof(ctypes.c_uint32) or size <= 0 or size % 4:
            raise ValueError("MMIO base and size must be positive and 32-bit aligned")

        page_size = mmap.PAGESIZE
        page_base = base & ~(page_size - 1)
        page_offset = base - page_base
        map_size = page_offset + size

        self._page_offset = page_offset
        self._fd = os.open("/dev/mem", os.O_RDWR | os.O_SYNC)
        self._mem = mmap.mmap(
            self._fd,
            map_size,
            mmap.MAP_SHARED,
            mmap.PROT_READ | mmap.PROT_WRITE,
            offset=page_base,
        )
        word_array = ctypes.c_uint32 * (map_size // ctypes.sizeof(ctypes.c_uint32))
        self._words = word_array.from_buffer(self._mem)

    def close(self) -> None:
        del self._words
        self._mem.close()
        os.close(self._fd)

    def __enter__(self) -> "DevMem32":
        return self

    def __exit__(self, _exc_type, _exc, _traceback) -> None:
        self.close()

    def read32(self, offset: int) -> int:
        self._check_offset(offset)
        return int(self._words[self._word_index(offset)])

    def write32(self, offset: int, value: int) -> None:
        self._check_offset(offset)
        # mmap slice assignment uses memcpy(), which may split or duplicate an
        # MMIO store. A typed native word assignment emits one 32-bit access.
        self._words[self._word_index(offset)] = value & 0xFFFF_FFFF

    def _word_index(self, offset: int) -> int:
        return (self._page_offset + offset) // ctypes.sizeof(ctypes.c_uint32)

    @staticmethod
    def _check_offset(offset: int) -> None:
        if offset < 0 or (offset % 4) != 0:
            raise ValueError(f"offset must be non-negative and 32-bit aligned: 0x{offset:x}")
        if offset > APB_SIZE - 4:
            raise ValueError(f"offset outside APB window: 0x{offset:x}")


def parse_int(text: str) -> int:
    return int(text, 0)


def read_text(path: str) -> str:
    with open(path, "r", encoding="ascii") as stream:
        return stream.read().rstrip("\0\n")


def preflight_platform(base: int) -> None:
    if base != APB_BASE:
        return

    try:
        firmware_name = read_text(FPGA_FIRMWARE_NAME_PATH)
    except OSError as exc:
        raise RuntimeError(
            "Q-Crate FPGA ownership cannot be verified; "
            "qcrate-pl-load.service is not ready"
        ) from exc

    if firmware_name != FPGA_FIRMWARE_NAME:
        raise RuntimeError(
            f"FPGA region contains {firmware_name!r}, expected {FPGA_FIRMWARE_NAME!r}"
        )

    for clock_name, path, expected in CLOCK_RATE_PATHS:
        try:
            actual = int(read_text(path), 10)
        except (OSError, ValueError) as exc:
            raise RuntimeError(f"cannot verify {clock_name} at {path}") from exc
        if abs(actual - expected) > CLOCK_RATE_TOLERANCE_HZ:
            raise RuntimeError(
                f"{clock_name} is {actual} Hz, expected {expected} Hz "
                f"(+/- {CLOCK_RATE_TOLERANCE_HZ} Hz)"
            )


def expect(actual: int, expected: int, what: str) -> None:
    if actual != expected:
        raise RuntimeError(f"{what}: expected 0x{expected:08x}, got 0x{actual:08x}")
    print(f"OK  {what:<28} 0x{actual:08x}")


def command_dump(regs: DevMem32) -> None:
    for reg in REGISTERS:
        value = regs.read32(reg.offset)
        print(f"0x{reg.offset:04x} {reg.name:<28} 0x{value:08x}")


def command_smoke(regs: DevMem32, skip_writes: bool) -> None:
    print("Q-Crate APB smoke test")
    expect(regs.read32(SYS_DEVICE_ID), DEVICE_ID_QCRT, "DEVICE_ID")
    expect(regs.read32(SYS_VERSION), VERSION_EXPECTED, "VERSION")
    print(f"OK  BUILD_ID                     0x{regs.read32(SYS_BUILD_ID):08x}")
    print(f"OK  CAPABILITIES                 0x{regs.read32(SYS_CAPABILITIES):08x}")
    expect(regs.read32(SYS_STREAM_CLOCK_HZ), STREAM_CLOCK_HZ, "STREAM_CLOCK_HZ")
    expect(regs.read32(SYS_CONTROL_CLOCK_HZ), CONTROL_CLOCK_HZ, "CONTROL_CLOCK_HZ")

    if skip_writes:
        print("SKIP writes requested; read-only smoke test complete")
        return

    original_scratch = regs.read32(SYS_SCRATCH)
    for pattern in (0x0000_0000, 0x5A5A_A5A5, 0x1234_5678):
        regs.write32(SYS_SCRATCH, pattern)
        expect(regs.read32(SYS_SCRATCH), pattern, f"SCRATCH write/read 0x{pattern:08x}")
    regs.write32(SYS_SCRATCH, original_scratch)

    regs.write32(STREAM_FRAME_LENGTH, 16)
    regs.write32(STREAM_FRAME_COUNT, 1)
    regs.write32(STREAM_MODE, 0)
    regs.write32(STREAM_CONTROL, 0)

    expect(regs.read32(STREAM_FRAME_LENGTH), 16, "STREAM.FRAME_LENGTH")
    expect(regs.read32(STREAM_FRAME_COUNT), 1, "STREAM.FRAME_COUNT")
    expect(regs.read32(STREAM_MODE), 0, "STREAM.STREAM_MODE")
    print(f"OK  STREAM.STATUS                0x{regs.read32(STREAM_STATUS):08x}")
    print("PASS qcrate_apb_smoke")


def main() -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--base", type=parse_int, default=APB_BASE,
                        help="physical APB base address, default: 0xA0010000")
    subparsers = parser.add_subparsers(dest="command", required=True)

    subparsers.add_parser("dump", help="read the known Q-Crate APB registers")

    smoke_parser = subparsers.add_parser("smoke", help="run identity and scratch tests")
    smoke_parser.add_argument("--skip-writes", action="store_true",
                              help="only read identity registers")

    read_parser = subparsers.add_parser("read", help="read one 32-bit APB offset")
    read_parser.add_argument("offset", type=parse_int)

    write_parser = subparsers.add_parser("write", help="write one 32-bit APB offset")
    write_parser.add_argument("offset", type=parse_int)
    write_parser.add_argument("value", type=parse_int)
    write_parser.add_argument(
        "--unsafe-sequence-owner-override",
        action="store_true",
        help="permit a diagnostic write to the R5-owned sequencer page",
    )

    args = parser.parse_args()

    try:
        preflight_platform(args.base)
    except RuntimeError as exc:
        print(f"qcrate-apb: refusing unsafe APB access: {exc}", file=sys.stderr)
        print(
            "qcrate-apb: inspect 'systemctl status qcrate-pl-load.service'",
            file=sys.stderr,
        )
        return 2

    if (
        args.command == "write"
        and SEQUENCE_BASE <= args.offset < SEQUENCE_PAGE_END
        and not args.unsafe_sequence_owner_override
    ):
        print(
            "qcrate-apb: sequencer writes are owned by R5; use qcrate-sequence",
            file=sys.stderr,
        )
        return 2

    with DevMem32(args.base, APB_SIZE) as regs:
        if args.command == "dump":
            command_dump(regs)
        elif args.command == "smoke":
            command_smoke(regs, args.skip_writes)
        elif args.command == "read":
            value = regs.read32(args.offset)
            print(f"0x{value:08x}")
        elif args.command == "write":
            regs.write32(args.offset, args.value)
        else:
            parser.error(f"unsupported command: {args.command}")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
